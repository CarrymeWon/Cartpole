hello,world

-----steps for uploading local repository to github-----------
1. git config --global user.name"xxxx"
   git config --global user.email"....."
(choose and enter a local file in terminal, which you want it to be sychronizied with github repository. Operations: set the right github account:)

2. git init (initialize a local file as repository)
3. git remote add hhh https://github.com/CarrymeWon/Cartpole.git
4. git status
5. git add *
6. git commit -m ''
7. git push -u hhh master
8: git config --global credential.helper store
(Solving problem of requiring username and password input every time pull a commit:)


 电脑文件 上传到Github上：

1. 在GitHub上创建一个新的仓库，命名为自己想要的名字， 复制仓库的ssh链接
2. 点击本地希望上传的文件夹右键选择 ‘git bash here’
   -命令行输入初始化命令'git init'
   -(如果想创建README.md可以在文件夹中手动创建) 输入命令'touch README.md'
   -依次使用以下命令将文件夹的文件全部添加到缓存区并提交：
      'git add .' // 点代表文件夹里的所有文件，如果要某一个文件，即将点替换成文件名
      'git commit -m 'first commit'' //first commit可以替换成其他注释名字
   -提交完成后，西药将远程仓库与本地同步链接
      'git remote add origin 复制仓库的链接'
   -最后 将本地仓库的内容推送到远程仓库
      'git push -u origin master'
      
      
